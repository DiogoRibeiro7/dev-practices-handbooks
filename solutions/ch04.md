# Chapter 04 Solutions

> ⚠️ **Spoiler alert:** These responses solve the Chapter 04 exercises.

1. KPI narrative: “Build stability dipped to 82% because the regression suite timed out on the ingestion pipeline; we reran the suite, identified a data-locking change, and plan a targeted integration fix.”
2. Alert rule: “If regression job fails twice within 10 minutes, post to quality channel, capture logs, and trigger triage call.” Document the channels and owners.
3. Run your mutation testing tool (e.g., `mutmut run`) and note the surviving mutations such as missing conditional branches.
4. Three SLI/SLO pairs: error rate (SLO 0.5% per hour), latency (SLO 200 ms P95), and availability (SLO 99.9% uptime). Explain the error budget as the remaining allowance before blocking deployments.
5. Dashboard sketch: timeline of coverage reviews, KPI gauges (build stability, defect escape), plus risk register entries in a side panel.
6. Tune thresholds by adding a warning at 70% CPU and a critical at 90%, documenting actions (investigate vs pause deployments).
7. The example script reads metrics and prints “Metrics healthy” or “open incident ticket” depending on stability; run it and note when you would halt a release (e.g., build_stability < 0.9).
8. Compose a narrative summary such as “Regression suite flaked twice due to shared fixture; we quarantined the test, refreshed the fixture, and scheduled a follow-up exploratory session,” then attach it near your KPI chart.
